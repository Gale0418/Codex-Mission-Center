# Incidents

Only create an `INC-xxx.md` record for a significant, evidenced event. Every record requires summary, impact, root cause, final fix, verification evidence, related task IDs, and status. Add symptoms, reproduction, rejected fixes, decision/commit links, and promoted lessons when they exist; explicitly write `None` when an applicable field was investigated but absent. Do not store raw logs, secrets, or a second task lifecycle here.
